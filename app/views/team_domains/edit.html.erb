<% content_for :breadcrumbs do %>
  <%= govuk_back_link(href: team_path(@team)) %>
<% end %>

<% title = "#{@team.errors.any? ? "Error: " : "" }Edit team domain" %>
<%= content_for :page_title, title %>

<%= render "form", team: @team %>
