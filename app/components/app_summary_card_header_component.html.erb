<header class="app-summary-card__header" <%= @anchor ? "id=#{@anchor}" : nil %>>
  <h<%= @heading_level %> class="app-summary-card__title govuk-!-font-weight-bold govuk-!-font-size-24">
    <%= @title %>
  </h<%= @heading_level %>>
  <%= content %>
</header>
